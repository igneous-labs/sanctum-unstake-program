{
  "name": "@unstake-it/sol",
  "version": "0.0.3",
  "repository": "github:igneous-labs/unstake",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [
    "solana",
    "staking"
  ],
  "author": "",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/igneous-labs/unstake",
  "dependencies": {
    "@project-serum/anchor": "0.24.2"
  },
  "devDependencies": {
    "@soceanfi/solana-stake-sdk": "^0.1.0",
    "@solana/spl-token": "^0.2.0",
    "@types/bn.js": "^5.1.0",
    "@types/node-fetch": "^2.6.2",
    "bn.js": "^5.2.1",
    "superstruct": "^0.14.2",
    "typedoc": "0.22.15",
    "typescript": "^4.3.5"
  },
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist && rm -f *.tgz",
    "rebuild": "cd ../ && yarn build && cd ts && yarn build",
    "prebuild": "readlink -f ../target/idl/*.json | xargs -I % ln -sf % src/idl/idl.json && readlink -f ../target/types/*.ts | xargs -I % ln -sf % src/idl/idl.ts",
    "build": "yarn tsc",
    "prepack": "yarn clean && yarn build",
    "docs": "typedoc --entryPoints src/index.ts"
  },
  "peerDependencies": {
    "@soceanfi/solana-stake-sdk": "^0.1.0",
    "@solana/spl-token": "^0.2.0"
  },
  "resolutions": {
    "@solana/web3.js": "1.43.5"
  }
}
